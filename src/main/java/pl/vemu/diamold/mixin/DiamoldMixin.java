package pl.vemu.diamold.mixin;

//@Mixin(MinecraftClient.class)
public class DiamoldMixin {

    /*@Inject(at = @At("TAIL"), method = "tick")
    private void onTick(CallbackInfo info) {
        MinecraftClient instance = MinecraftClient.getInstance();
        if (instance.world != null) instance.world.getPlayers().forEach(playerEntity -> playerEntity.setGlowing(true));
    }*/
}
